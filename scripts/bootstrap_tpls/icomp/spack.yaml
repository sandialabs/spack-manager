spack:
  include:
  - packages.yaml
  - compilers.yaml
  - config.yaml
  # build the latest gcc we can with the base compiler
  specs:
  - gcc
  view: false
  packages:
    all:
      variants: [build_type=Release libs=static ~shared]
    gcc:
      variants: [~bootstrap ~profiled +strip]
      
